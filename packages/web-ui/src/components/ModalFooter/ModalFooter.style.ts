import { makeStyles } from "@material-ui/core";

export const useStyles = makeStyles({
  container: {
    width: "100%",
  },
  account: {
    marginTop: 24,
    fontSize: 15,
  },
  accountLink: {
    color: "#2539DEDE",
    fontSize: 15,
  },
});
