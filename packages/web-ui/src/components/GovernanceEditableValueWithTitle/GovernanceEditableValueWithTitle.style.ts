import { makeStyles } from "@material-ui/core";

export const useStyles = makeStyles(() => ({
  saveButton: {
    marginLeft: "auto",
    fontSize: 24,
    padding: 0,
  },
  iconButton: {
    padding: 0,
    marginLeft: 8,
  },
}));
