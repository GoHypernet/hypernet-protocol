import { makeStyles } from "@material-ui/core";

export const useStyles = makeStyles({
  wrapper: {
    display: "flex",
    alignItems: "center",
  },
  form: {
    flex: 1,
  },
  label: {
    marginRight: 16,
    marginBottom: 24,
  },
});
