import { makeStyles } from "@material-ui/core";

export const useStyles = makeStyles({
  descriptionContainer: {
    display: "flex",
    alignItems: "center",
  },
  descriptionText: {
    marginLeft: 16,
  },
});
