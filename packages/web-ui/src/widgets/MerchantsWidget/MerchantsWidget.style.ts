import { makeStyles } from "@material-ui/core";

export const useStyles = makeStyles({
  balancesWrapper: {
    margin: "25px 0",
  },
  balancesLabel: {
    fontSize: 20,
    marginBottom: 24,
    textAlign: "center",
  },
});
