import React, { useEffect } from "react";

import PageWrapper from "@governance-app/components/PageWrapper";

const Vote: React.FC = () => {
  return <PageWrapper label="Vote">Vote</PageWrapper>;
};

export default Vote;
