import React from "react";
import ReactDOM from "react-dom";

import MainContainer from "@governance-app/containers/MainContainer";

ReactDOM.render(
  <MainContainer />,
  document.getElementById("root") as HTMLElement,
);
