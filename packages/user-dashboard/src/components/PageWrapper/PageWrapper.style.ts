import { makeStyles } from "@material-ui/core";

export const useStyles = makeStyles({
  wrapper: {
    padding: "40px 24px",
    maxWidth: 960,
    margin: "auto",
  },
  label: {
    fontSize: 24,
    marginBottom: 30,
  },
  governanceWrapper: {
    padding: "40px 200px",
    backgroundColor: "#ffffff",
  },
});
